# 更新日志

## 1.0.0

- 支持在切换对话后保持 Codex Desktop 右侧工作面板打开。
- 增加 `只打开`、`侧边聊天`、`浏览器`、`终端` 四种目标模式。
- 开启保持按钮时默认选择 `只打开`，避免立即自动切换工具。
- 关闭保持按钮时，侧边聊天、浏览器和终端显示提示，不会强行打开。
- 增加“一次只保留一个右侧标签”，并加入主对话路由保护。
- 新建对话时只保持右侧栏状态，不会自动拉回旧对话。
- 增加浏览器网址处理，支持 `此聊天网址`、按对话记住上次选择的网址，以及合并当前对话和已有缓存。
- 浏览器网址缓存优先显示网页标题，并过滤对话正文、终端提示、错误页标题等误识别内容。
- 增加紧凑设置面板、状态反馈、主题切换和面板置顶。
