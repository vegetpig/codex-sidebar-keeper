# 更新日志

## 1.0.0

第一版把重点放在一件事上：切换对话时，右侧工作区别乱跑。

- 顶部栏新增 `Sidebar Keeper` 入口。
- 可以只展开右侧栏，也可以固定到侧边聊天、浏览器或终端。
- 浏览器网址默认先用 `此聊天网址` 兜底，选过具体网页后再按对话记住。
- 右侧标签可以只保留当前目标，避免侧边聊天、浏览器、终端越堆越多。
- 新建对话时不会自动拉回旧对话。
- 面板支持深色/浅色、拖动、置顶和状态提示。
