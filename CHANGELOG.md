# 更新日志

## 1.0.0

首个版本聚焦于右侧工作区的状态保持和标签整理。

- 在顶部栏添加 `Sidebar Keeper` 入口。
- 支持 `只打开`、`侧边聊天`、`浏览器`、`终端` 四种目标模式。
- 浏览器网址默认以 `此聊天网址` 作为兜底，用户选择具体网页后按对话保存。
- 可选择只保留一个右侧标签，减少侧边聊天、浏览器、终端之间的标签堆叠。
- 新建对话时避免自动回到旧对话。
- 设置面板支持深色/浅色、拖动、置顶和状态提示。
- 浏览器网址缓存会过滤命令输出中的 API 地址和脚本模板变量，避免误收进下拉框。
- 放宽工具选择菜单识别，侧边聊天等小菜单项也能被正确打开。
